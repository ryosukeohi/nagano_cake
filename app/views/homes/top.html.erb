<h1>新着商品</h1>

<% @items.each do |item| %>
<%= link_to item_path(item.id) do %>
　<%= attachment_image_tag item, :image, :fill, 60, 60 %></br>
<% end %>
  商品名 <%= item.name %></br>
  ¥<%= item.price %>
<% end %>